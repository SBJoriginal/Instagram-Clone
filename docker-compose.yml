version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: ugram_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: equipe22
      POSTGRES_DB: ugram_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - ugram-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    container_name: ugram_backend
    environment:
      - ASPNETCORE_ENVIRONMENT=Docker
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=db;Database=ugram_dev;Username=postgres;Password=equipe22
    ports:
      - "8080:8080"
    volumes:
      - ./backend/backend/uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
    networks:
      - ugram-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: ../docker/frontend.Dockerfile
    container_name: ugram_frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
    networks:
      - ugram-network
    restart: unless-stopped

volumes:
  postgres_data:


networks:
  ugram-network:
    driver: bridge
